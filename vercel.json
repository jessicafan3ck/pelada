{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ],
  "functions": {
    "api/render.ts": { "maxDuration": 60 },
    "api/render-status.ts": { "maxDuration": 30 }
  }
}
