{
  "name": "pelada-render",
  "private": true,
  "description": "Remotion project for Pelada MP4 rendering on AWS Lambda (pay-per-render). The composition reuses the web app's TemplateRenderer from ../src.",
  "scripts": {
    "deploy:fn": "remotion lambda functions deploy",
    "deploy:site": "remotion lambda sites create remotion/index.ts --site-name=pelada",
    "deploy": "npm run deploy:fn && npm run deploy:site",
    "studio": "remotion studio remotion/index.ts"
  },
  "dependencies": {
    "@remotion/cli": "4.0.484",
    "@remotion/lambda": "4.0.484",
    "remotion": "4.0.484",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.1"
  }
}
